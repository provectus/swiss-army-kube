provider: aws
policy: upsert-only

aws.region: us-east-1
aws.assumeRoleArn: setarns

domainFilters:
  - local
sources:
  - ingress
  - service
rbac:
  create: true
securityContext:
  fsGroup: 65534 # For ExternalDNS to be able to read Kubernetes and AWS token files